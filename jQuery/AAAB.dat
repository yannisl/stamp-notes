 <!--
{{feature: .. Simplicity the art of maximizing the amount of work not done. The Agile Manifesto}}

{{keywords: CodeIgniter,PHP, CodeIgniter, File helper class}}
{{author:Dr Y Lazarides}}
{{date:8 September 2008}}
{{category: PHP, MVC, Parsers}}
{{snippet:using CodeIgniter's file_helper class}}

-->

###Ideas for Further Development

Now that I have been thinking - already joined the Stanford course for Artificial Intelligence - I need to come-up with some ideas as to how to develop a Project or two around this.

####Cyprus Instant News

Use [NY Times API](http://prototype.nytimes.com/gst/apitool/index.html?api_id=1&request_id=3&resp_format=json&perform_request=Make+Request&use_pp=on) to check for latest news related to Cyprus. Supplement this with Cyprus local Papers, Greece etc. Do some semantic analysis using other APIs etc. Add some feedback loop from users for training the algorithms as to what content to promote up.

Use events type reporting with map API to get incident reports, weather from Yahoo and local news. 

Add 'web agents' to linkify what is available at Wikipedia etc. Limit linkification to topics of interest. 

Compare and capture Google ads to see if we are in agreement with Google:).

Add feeds API module to assist, blog etc.

Tools for editor.

Limit everything to English First -> Chinese room -> to English.

Remove bias


###How does this relate to AI?

This is actually a 'weak AI' that answers some questions very well, What's news from Cyprus? It can also measure popularity of some politicians. trends if it captures and cache's historical data.

###Money proposition

Advertizing money. Influence buying. Fame.




























 <script type='text/javascript' src='http://www.google.com/jsapi'></script>
  <script type='text/javascript'>
   google.load('visualization', '1', {'packages': ['geomap']});
   google.setOnLoadCallback(drawMap);

    function drawMap() {
      var data = new google.visualization.DataTable();
      data.addRows(6);
      data.addColumn('string', 'Country');
      data.addColumn('number', 'Popularity');
      data.setValue(0, 0, 'Germany');
      data.setValue(0, 1, 200);
      data.setValue(1, 0, 'United States');
      data.setValue(1, 1, 300);
      data.setValue(2, 0, 'Brazil');
      data.setValue(2, 1, 400);
      data.setValue(3, 0, 'Canada');
      data.setValue(3, 1, 500);
      data.setValue(4, 0, 'France');
      data.setValue(4, 1, 600);
      data.setValue(5, 0, 'RU');
      data.setValue(5, 1, 700);

      var options = {};
      options['dataMode'] = 'regions';

      var container = document.getElementById('map_canvas');
      var geomap = new google.visualization.GeoMap(container);
      geomap.draw(data, options);
  };
  </script>



  <div id='map_canvas'></div>






<h2>Enumerating properties</h2>


<div  class="code-block console-warp"><div class="code code-block"><code>
$('#ny').load('http://api.nytimes.com/svc/books/v2/lists/names.json?api-key=####');

</code>
</div>
<p class="msg">
</p>

<button id="ny" class="eval">Run Code!</button>
<ol class="results" style="white-space:pre"></ol> 
</div>




<hr/>



##Further Reading

http://xregexp.com/syntax/


                                                                                      