


<h2>Priority Queue</h2>

Closure's PriorityQueue constructor, will construct a priority queue. It also inherits all the methods from Heap, which is quite convenient. If you do not remember what a priority queue is you can have a quick read of some details below the console.

\begin{codeblock}

var q = new goog.structs.PriorityQueue();

var priority = 3;
var value = 4;
q.enqueue(3, value);
q.enqueue(1, 3);
q.enqueue(10, 9);
q.enqueue(1, 3);
q.enqueue(4, 4);
q.enqueue(1, 8);
log('last value added', q.getKeys());

q.dequeue();

log('first de-queue', q.getKeys());
q.dequeue();
log(q.getKeys());

//log(q.getValues());
\end{codeblock}





<span id="chapter" class="hidden">11</span>
##Further Reading





  
  







  