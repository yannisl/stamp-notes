 <!--
{{feature: .. Simplicity the art of maximizing the amount of work not done. The Agile Manifesto}}

{{keywords: CodeIgniter,PHP, CodeIgniter, File helper class}}
{{author:Dr Y Lazarides}}
{{date:8 September 2008}}
{{category: PHP, MVC, Parsers}}
{{snippet:using CodeIgniter's file_helper class}}

-->

<h2>Standard Library flatten() method</h2>

The method takes one or more arrays and flattens the results.

<div  class="code-block console-wrap">
<div class="code-block code">
<code>
/**
 * Returns an array consisting of every argument with all arrays
 * expanded in-place recursively.
 *
 * @param {*} var_args The values to flatten.
 * @return {!Array.<*>} An array containing the flattened values.
 */
function flatten(var_args) {
  var result = [];
  for (var i = 0; i < arguments.length; i++) {
    var element = arguments[i];
    if (goog.isArray(element)) {
      flatten.apply(result, flatten.apply(null, element));
    } else {
      result.push(element);
    }
  }
  return result;
};

var b=['a','b','c',[1,2,3,4]];

var d = goog.array.flatten(b);

log('the flattened array ', d);


</code>
</div>

<div  id ="msg" class="msg">
</div>
<div class="console"></div>
<button class="eval">Run Code!</button>
<ol class="results"></ol> 
</div>


<h3>fill numeric</h3>
The fillNumeric() method returns an array filled with numbers in ascending order.

<div  class="code-block console-wrap">
<div class="code-block code">
<code>
/**
 * Returns an array consisting of a list of  n numbers in ascending order from start to n.
 *
 * @param {*} start the start number.
 * @param {number} n The repeat count.
 * @return {!Array.<*>} An array with the repeated value.
 */

function fillNumeric(start, n) {
  var array = [];
  if (arguments.length < 2) {
    var n = start;
    var start = 0;
  }
  for (var i = start; i < n - start; i++) {
    array[i] = i;
  }
  return array;
};

log(fillNumeric(4, 12));
log(fillNumeric(13));
</code>
</div>

<div  id ="msg" class="msg">
</div>
<div class="console"></div>
<button class="eval">Run Code!</button>
<ol class="results"></ol> 
</div>


<span id="chapter" class="hidden">11</span>
##Further Reading





  
  







        